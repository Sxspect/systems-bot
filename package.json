{
  "name": "discord-render-bot",
  "version": "1.0.0",
  "description": "A Discord server bot ready to host on Render.",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "register": "node scripts/register-commands.js",
    "check": "node --check src/index.js && node --check src/commands.js && node --check src/serverSetup.js && node --check scripts/register-commands.js"
  },
  "dependencies": {
    "discord.js": "^14.18.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
